apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: s2i-python-27-ubi8-run
spec:
  params:
    - name: VERSION
      value: "2.7-ubi8"
  pipelineRef:
    name: s2i-python-pipeline
  timeout: 10m
  workspaces:
  - name: source
    persistentVolumeClaim:
      claimName: shared-pvc