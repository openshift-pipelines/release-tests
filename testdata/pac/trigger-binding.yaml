apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerBinding
metadata:
  name: merge-request-binding
spec:
  params:
    - name: git-repository-url
      value: $(body.project.git_http_url)
    - name: git-revision
      value: $(body.object_attributes.last_commit.id)
