apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: s2i-ruby-30-ubi8-run
spec:
  params:
    - name: VERSION
      value: "3.0-ubi8"
  pipelineRef:
    name: s2i-ruby-pipeline
  timeout: 10m
  workspaces:
  - name: source
    persistentVolumeClaim:
      claimName: shared-pvc