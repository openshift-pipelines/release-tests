apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: s2i-go-1167-ubi8-run
spec:
  params:
  - name: VERSION
    value: 1.16.7-ubi8
  pipelineRef:
    name: s2i-go-pipeline
  timeout: 10m
  workspaces:
  - name: source
    persistentVolumeClaim:
      claimName: shared-pvc