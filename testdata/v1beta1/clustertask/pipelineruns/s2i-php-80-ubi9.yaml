apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: s2i-php-80-ubi9-run
spec:
  params:
  - name: VERSION
    value: "8.0-ubi9"
  pipelineRef:
    name: s2i-php-pipeline
  timeout: 10m
  workspaces:
  - name: source
    persistentVolumeClaim:
      claimName: shared-pvc