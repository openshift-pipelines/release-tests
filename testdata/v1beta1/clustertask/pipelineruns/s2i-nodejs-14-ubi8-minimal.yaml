apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: s2i-nodejs-14-ubi8-minimal-run
spec:
  params:
  - name: VERSION
    value: "14-ubi8-minimal"
  pipelineRef:
    name: s2i-nodejs-pipeline
  timeout: 12m
  workspaces:
  - name: source
    persistentVolumeClaim:
      claimName: shared-pvc