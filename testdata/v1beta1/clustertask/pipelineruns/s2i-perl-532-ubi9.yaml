apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: s2i-perl-532-ubi9-run
spec:
  params:
    - name: VERSION
      value: "5.32-ubi9"
  pipelineRef:
    name: s2i-perl-pipeline
  timeout: 10m
  workspaces:
  - name: source
    persistentVolumeClaim:
      claimName: shared-pvc