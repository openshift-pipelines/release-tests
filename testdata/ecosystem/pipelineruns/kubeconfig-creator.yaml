apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  name: kubeconfig-creator-pipeline-run
spec:
  pipelineRef:
    name: kubeconfig-creator-pipeline
  workspaces:
  - name: source
    persistentVolumeClaim:
      claimName: shared-pvc
  timeouts: 
    pipeline: 10m
