apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: s2i-python-39-ubi9-run
spec:
  params:
    - name: VERSION
      value: "3.9-ubi9"
  pipelineRef:
    name: s2i-python-pipeline
  timeout: 10m
  workspaces:
  - name: source
    persistentVolumeClaim:
      claimName: shared-pvc