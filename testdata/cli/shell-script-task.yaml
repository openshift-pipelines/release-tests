apiVersion: tekton.dev/v1alpha1
kind: Task
metadata:
  name: shell-script-task
spec:
  steps:
    - name: shell-example
      image: ubuntu  # contains python
      script: |
        echo "Hello from Bash!"