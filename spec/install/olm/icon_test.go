package olm

import (
	"testing"

	. "github.com/smartystreets/goconvey/convey"
)

func TestNewIcon(t *testing.T) {

	Convey("Given that you are on operators install page ", t, func() {
		Convey("When I look for OpenShift Pipelines ", func() {
			Convey("I should able to see the new icon", nil)
		})
	})
}
