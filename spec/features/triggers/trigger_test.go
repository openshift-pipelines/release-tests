package triggers

import (
	. "github.com/smartystreets/goconvey/convey"
	"testing"
)

func TestRunTriggerTutorial(t *testing.T) {

	Convey("Given that Operator is installed", t, func() {
		Convey("I should be able to run Triggers Tutorial as a non admin", nil)
	})
}
